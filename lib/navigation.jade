nav.navbar.navbar-inverse.visible-xs(role='navigation')
  .container
    .navbar-header
      .navbar-header-info
        span.glyphicon.glyphicon-earphone(aria-hidden='true')
        | #{phoneNumber}
      button.navbar-toggle.collapsed(type='button', data-toggle='collapse', data-target='#navbar', aria-expanded='false', aria-controls='navbar')
        span.sr-only Toggle navigation
        span.icon-bar
        span.icon-bar
        span.icon-bar
    #navbar.collapse.navbar-collapse
      ul.nav.navbar-nav
        for page, key in pages
          li(class=currentPage === key ? "active" : "")
            a(href= key) #{page.title}
        li.navbar-info
          a(href='#')
            span.glyphicon.glyphicon-earphone(aria-hidden='true')
            | #{phoneNumber}
